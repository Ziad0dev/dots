# SwayFX: Input configuration (kept minimal to avoid conflicts with existing sway config)

input * {
  xkb_layout us
  repeat_delay 280
  repeat_rate 40
}

input "type:touchpad" {
  tap enabled
  natural_scroll enabled
  middle_emulation enabled
}