# SwayFX: App specific behaviors (non-destructive)
# Adapt or remove entries based on your workflow.

# Workspace assignment examples
assign [class="Firefox"] 2
assign [class="discord"] 4

# Floating utility examples
for_window [app_id="pavucontrol"] floating enable, move position 60px 60px
for_window [title="^Open File$"] floating enable

# Scratchpad handled in main config (swayfx/config)