#!/usr/bin/env python3
import json
import sys
from pathlib import Path

RISE = Path(__file__).resolve().parent.parent / "config/quickshell/rise"

PAYLOAD = r"""{"new": {"modules/BarSweep.qml": "import QtQuick\n\nRectangle {\n    id: sweep\n\n    required property var theme\n    property color tint: sweep.theme.seal\n    property real band: 0.13\n    property real strength: 0.0\n    property real pos: -0.4\n    property int  travelDuration: 760\n    property bool reverse: false\n\n    function run() { sweepAnim.restart() }\n\n    color: \"transparent\"\n    opacity: sweep.strength\n    visible: sweep.strength > 0.004\n    z: 1\n\n    readonly property color edge: Qt.rgba(sweep.tint.r, sweep.tint.g, sweep.tint.b, 0.0)\n    readonly property color core: Qt.rgba(sweep.tint.r, sweep.tint.g, sweep.tint.b, 0.38)\n\n    gradient: Gradient {\n        orientation: Gradient.Horizontal\n        GradientStop { position: Math.max(0, Math.min(1, sweep.pos - sweep.band)); color: sweep.edge }\n        GradientStop { position: Math.max(0, Math.min(1, sweep.pos));              color: sweep.core }\n        GradientStop { position: Math.max(0, Math.min(1, sweep.pos + sweep.band)); color: sweep.edge }\n    }\n\n    ParallelAnimation {\n        id: sweepAnim\n        NumberAnimation {\n            target: sweep; property: \"pos\"\n            from: sweep.reverse ? 1.0 + sweep.band : -sweep.band\n            to:   sweep.reverse ? -sweep.band : 1.0 + sweep.band\n            duration: sweep.travelDuration\n            easing.type: Easing.InOutQuad\n        }\n        SequentialAnimation {\n            NumberAnimation { target: sweep; property: \"strength\"; from: 0.0; to: 1.0; duration: 140 }\n            PauseAnimation  { duration: Math.max(0, sweep.travelDuration - 420) }\n            NumberAnimation { target: sweep; property: \"strength\"; to: 0.0; duration: 280 }\n        }\n    }\n}\n", "modules/PillMotion.qml": "import QtQuick\n\nItem {\n    id: motion\n\n    property bool active: true\n    property real hoverScale: 1.055\n    property real pressScale: 0.945\n    property int  introDelay: 0\n    property bool introEnabled: true\n\n    readonly property bool hovered: motion.active && hh.hovered\n    readonly property bool held: motion.active && ph.active\n\n    property real amount: motion.held ? motion.pressScale : motion.hovered ? motion.hoverScale : 1.0\n    property real intro: motion.introEnabled ? 0.0 : 1.0\n\n    readonly property real scaleFactor: motion.amount * (0.86 + 0.14 * motion.intro)\n    readonly property real introOpacity: motion.intro\n\n    Behavior on amount {\n        SpringAnimation { spring: 5.0; damping: 0.34; mass: 0.55; epsilon: 0.0005 }\n    }\n\n    HoverHandler { id: hh; enabled: motion.active }\n    PointHandler { id: ph; enabled: motion.active; acceptedButtons: Qt.LeftButton }\n\n    SequentialAnimation {\n        running: motion.introEnabled\n        PauseAnimation { duration: motion.introDelay }\n        NumberAnimation {\n            target: motion; property: \"intro\"\n            from: 0.0; to: 1.0; duration: 460; easing.type: Easing.OutBack; easing.overshoot: 1.1\n        }\n    }\n}\n", "modules/PulseRing.qml": "import QtQuick\n\nRectangle {\n    id: ring\n\n    required property var theme\n    property color tint: ring.theme.seal\n    property real peak: 1.34\n    property int  duration: 520\n\n    function pulse() { pulseAnim.restart() }\n\n    anchors.centerIn: parent\n    width: parent ? parent.width : 0\n    height: parent ? parent.height : 0\n    radius: parent && parent.radius !== undefined ? parent.radius : height / 2\n    color: \"transparent\"\n    border.width: 1.5\n    border.color: ring.tint\n    opacity: 0\n    scale: 1\n    z: -2\n\n    ParallelAnimation {\n        id: pulseAnim\n        NumberAnimation {\n            target: ring; property: \"scale\"\n            from: 1.0; to: ring.peak; duration: ring.duration; easing.type: Easing.OutCubic\n        }\n        SequentialAnimation {\n            NumberAnimation { target: ring; property: \"opacity\"; from: 0.0; to: 0.6; duration: 80 }\n            NumberAnimation { target: ring; property: \"opacity\"; to: 0.0; duration: ring.duration - 80; easing.type: Easing.InCubic }\n        }\n    }\n}\n", "modules/RollingNumber.qml": "import QtQuick\n\nItem {\n    id: rn\n\n    property int value: 0\n    property int digits: 2\n    property color tint: \"white\"\n    property string family: \"monospace\"\n    property int pixelSize: 12\n    property int weight: Font.Normal\n    property string suffix: \"\"\n    property bool rolling: true\n    property int rollDuration: 380\n    property int rollStagger: 55\n\n    readonly property real cellW: Math.ceil(probe.implicitWidth)\n    readonly property real cellH: Math.ceil(probe.implicitHeight)\n\n    implicitWidth: rn.digits * rn.cellW + (rn.suffix === \"\" ? 0 : Math.ceil(suffixText.implicitWidth))\n    implicitHeight: rn.cellH\n\n    Text {\n        id: probe\n        visible: false\n        text: \"0\"\n        font.family: rn.family\n        font.pixelSize: rn.pixelSize\n        font.weight: rn.weight\n    }\n\n    Row {\n        anchors.verticalCenter: parent.verticalCenter\n        spacing: 0\n\n        Repeater {\n            model: rn.digits\n\n            delegate: Item {\n                id: cell\n                required property int index\n                readonly property int place: rn.digits - 1 - cell.index\n                readonly property int digit: Math.floor(Math.abs(rn.value) / Math.pow(10, cell.place)) % 10\n\n                property real pos: cell.digit\n\n                width: rn.cellW\n                height: rn.cellH\n                clip: true\n\n                onDigitChanged: cell.roll()\n\n                function roll() {\n                    var t = cell.digit\n                    while (t < cell.pos - 5) t += 10\n                    while (t > cell.pos + 5) t -= 10\n                    rollAnim.dest = t\n                    rollAnim.restart()\n                }\n\n                function normalize() {\n                    var v = cell.pos\n                    while (v >= 10) v -= 10\n                    while (v < 0) v += 10\n                    cell.pos = v\n                }\n\n                SequentialAnimation {\n                    id: rollAnim\n                    property real dest: 0\n                    PauseAnimation { duration: rn.rolling ? cell.place * rn.rollStagger : 0 }\n                    NumberAnimation {\n                        target: cell; property: \"pos\"\n                        to: rollAnim.dest\n                        duration: rn.rolling ? rn.rollDuration : 0\n                        easing.type: Easing.OutBack; easing.overshoot: 1.05\n                    }\n                    ScriptAction { script: cell.normalize() }\n                }\n\n                Column {\n                    y: -(cell.pos + 10) * rn.cellH\n                    Repeater {\n                        model: 30\n                        delegate: Text {\n                            required property int index\n                            width: rn.cellW\n                            height: rn.cellH\n                            renderType: rn.rolling ? Text.QtRendering : Text.NativeRendering\n                            text: String(index % 10)\n                            color: rn.tint\n                            font.family: rn.family\n                            font.pixelSize: rn.pixelSize\n                            font.weight: rn.weight\n                            horizontalAlignment: Text.AlignHCenter\n                            verticalAlignment: Text.AlignVCenter\n                        }\n                    }\n                }\n            }\n        }\n\n        Text {\n            id: suffixText\n            anchors.verticalCenter: parent.verticalCenter\n            renderType: rn.rolling ? Text.QtRendering : Text.NativeRendering\n            visible: rn.suffix !== \"\"\n            text: rn.suffix\n            color: rn.tint\n            font.family: rn.family\n            font.pixelSize: rn.pixelSize\n            font.weight: rn.weight\n        }\n    }\n}\n"}, "edits": {"BarSlot.qml": [["                opacity: autoShown ? 1 : 0\n                Behavior on opacity { NumberAnimation { duration: 140; easing.type: Easing.OutCubic } }\n                Behavior on width { NumberAnimation { duration: 200; easing.type: Easing.OutCubic } }\n                Loader {\n                    id: ldr\n                    x: slot.pad\n                    anchors.verticalCenter: parent.verticalCenter\n                    sourceComponent: barSlot.registry[slot.gid]\n                    // dim the original while its ghost is being dragged\n                    opacity: (barSlot.dragItem === ldr && barSlot.dragActive) ? 0.25 : 1.0\n                }\n                // \u2500\u2500 drag-catcher: only in unlock mode, overlays the widget \u2500\u2500\n                MouseArea {", "                opacity: autoShown ? 1 : 0\n                Behavior on opacity { NumberAnimation { duration: 140; easing.type: Easing.OutCubic } }\n                Behavior on width { NumberAnimation { duration: 200; easing.type: Easing.OutCubic } }\n                PillMotion {\n                    id: motion\n                    anchors.fill: parent\n                    active: barSlot.root.motionHover && !barSlot.root.barUnlocked\n                    introDelay: 70 + slot.index * 45\n                }\n                Loader {\n                    id: ldr\n                    x: slot.pad\n                    anchors.verticalCenter: parent.verticalCenter\n                    sourceComponent: barSlot.registry[slot.gid]\n                    transformOrigin: barSlot.root.barPosition === \"bottom\" ? Item.Bottom : Item.Top\n                    scale: motion.scaleFactor\n                    // dim the original while its ghost is being dragged\n                    opacity: ((barSlot.dragItem === ldr && barSlot.dragActive) ? 0.25 : 1.0) * motion.introOpacity\n                }\n                // \u2500\u2500 drag-catcher: only in unlock mode, overlays the widget \u2500\u2500\n                MouseArea {"], ["            }\n        }\n\n        // \u2500\u2500 gap particle animation (flows in split gaps only when enabled) \u2500\u2500\n        Item {\n            id: particleLayer", "            }\n        }\n\n        Repeater {\n            model: island.runs\n            delegate: BarSweep {\n                id: runSweep\n                required property var modelData\n                theme: barSlot.root\n                x: runSweep.modelData.x\n                y: 0\n                width: Math.max(0, runSweep.modelData.w)\n                height: island.height\n                radius: barSlot.root.islandRadius\n                z: 1\n                Connections {\n                    target: barSlot.root\n                    function onBarPulse(kind) {\n                        if (barSlot.root.motionSweep && kind !== \"\") runSweep.run()\n                    }\n                }\n            }\n        }\n\n        // \u2500\u2500 gap particle animation (flows in split gaps only when enabled) \u2500\u2500\n        Item {\n            id: particleLayer"]], "Theme.qml": [["    // \u2500\u2500 workspace display mode \u2500\u2500\n    property string workspaceMode: \"10\"   // \"10\", \"5\", \"active\"\n    // \u2500\u2500 workspace display style (orthogonal to mode; persisted) \u2500\u2500\n    property string workspaceStyle: \"default\"   // \"default\", \"numbers\", \"magic\"\n\n    // \u2500\u2500 bar screen position (persisted) \u2500\u2500\n    property string barPosition: \"top\"   // \"top\" or \"bottom\"", "    // \u2500\u2500 workspace display mode \u2500\u2500\n    property string workspaceMode: \"10\"   // \"10\", \"5\", \"active\"\n    // \u2500\u2500 workspace display style (orthogonal to mode; persisted) \u2500\u2500\n    property string workspaceStyle: \"default\"   // \"default\", \"numbers\", \"magic\", \"comet\"\n\n    // \u2500\u2500 motion (persisted) \u2500\u2500\n    property bool motionHover:  true\n    property bool motionSweep:  true\n    property bool motionDigits: true\n    signal barPulse(string kind)\n\n    // \u2500\u2500 bar screen position (persisted) \u2500\u2500\n    property string barPosition: \"top\"   // \"top\" or \"bottom\""], ["    onStyleRadiusSmallChanged: if (_widgetsLoaded) saveWidgets()\n    onWorkspaceStyleChanged:   if (_widgetsLoaded) saveWidgets()\n    onBarPositionChanged:      if (_widgetsLoaded) saveWidgets()\n\n    function saveWidgets() {\n        var line = (modMemory    ? \"1\" : \"0\") + \" \"", "    onStyleRadiusSmallChanged: if (_widgetsLoaded) saveWidgets()\n    onWorkspaceStyleChanged:   if (_widgetsLoaded) saveWidgets()\n    onBarPositionChanged:      if (_widgetsLoaded) saveWidgets()\n    onMotionHoverChanged:      if (_widgetsLoaded) saveWidgets()\n    onMotionSweepChanged:      if (_widgetsLoaded) saveWidgets()\n    onMotionDigitsChanged:     if (_widgetsLoaded) saveWidgets()\n\n    function saveWidgets() {\n        var line = (modMemory    ? \"1\" : \"0\") + \" \""], ["                 + (compactPower      ? \"1\" : \"0\") + \" \"  // +30\n                 + (archBadgeShell    ? \"1\" : \"0\") + \" \"  // +31 updater shell badge\n                 + (compactMpris      ? \"1\" : \"0\") + \" \"  // +32 V2 FULL / muse presentation\n                 + (modGithub         ? \"1\" : \"0\")        // +33 github inbox pill\n        widgetSaveProc.command = [\"bash\", \"-c\",\n            \"echo '\" + line + \"' > '\" + widgetsCachePath + \"'\"]\n        widgetSaveProc.running = false", "                 + (compactPower      ? \"1\" : \"0\") + \" \"  // +30\n                 + (archBadgeShell    ? \"1\" : \"0\") + \" \"  // +31 updater shell badge\n                 + (compactMpris      ? \"1\" : \"0\") + \" \"  // +32 V2 FULL / muse presentation\n                 + (modGithub         ? \"1\" : \"0\") + \" \"  // +33 github inbox pill\n                 + (motionHover       ? \"1\" : \"0\") + \" \"  // +34\n                 + (motionSweep       ? \"1\" : \"0\") + \" \"  // +35\n                 + (motionDigits      ? \"1\" : \"0\")        // +36\n        widgetSaveProc.command = [\"bash\", \"-c\",\n            \"echo '\" + line + \"' > '\" + widgetsCachePath + \"'\"]\n        widgetSaveProc.running = false"], ["                    if (parts.length > wsField + 31) theme.archBadgeShell    = parts[wsField + 31] !== \"0\"\n                    if (parts.length > wsField + 32) theme.compactMpris      = parts[wsField + 32] === \"1\"\n                    if (parts.length > wsField + 33) theme.modGithub         = parts[wsField + 33] !== \"0\"\n                }\n                theme._widgetsLoaded = true\n            }", "                    if (parts.length > wsField + 31) theme.archBadgeShell    = parts[wsField + 31] !== \"0\"\n                    if (parts.length > wsField + 32) theme.compactMpris      = parts[wsField + 32] === \"1\"\n                    if (parts.length > wsField + 33) theme.modGithub         = parts[wsField + 33] !== \"0\"\n                    if (parts.length > wsField + 34) theme.motionHover       = parts[wsField + 34] !== \"0\"\n                    if (parts.length > wsField + 35) theme.motionSweep       = parts[wsField + 35] !== \"0\"\n                    if (parts.length > wsField + 36) theme.motionDigits      = parts[wsField + 36] !== \"0\"\n                }\n                theme._widgetsLoaded = true\n            }"]], "modules/AudioWidget.qml": [["        border.color: root.widgetBorderColor(rootMod.gid)\n        border.width: root.widgetBorderWidth(rootMod.gid)\n        PillShadow { theme: root }\n    }\n\n    Row {\n        id: row", "        border.color: root.widgetBorderColor(rootMod.gid)\n        border.width: root.widgetBorderWidth(rootMod.gid)\n        PillShadow { theme: root }\n        PulseRing {\n            id: volRing\n            theme: root\n            tint: rootMod.muted ? root.color01 : root.seal\n        }\n    }\n\n    onVolumeChanged: if (root.motionHover) volRing.pulse()\n    onMutedChanged:  if (root.motionHover) volRing.pulse()\n\n    Row {\n        id: row"]], "modules/CpuWidget.qml": [["            }\n        }\n\n        UiText {\n            anchors.verticalCenter: parent.verticalCenter\n            visible: !root.compactCpu\n            text: String(Math.min(100, rootMod.percent)).padStart(2, '0') + \"%\"\n            color: root.seal\n            font.family: root.mono\n            font.pixelSize: 12\n        }\n\n        IconText {", "            }\n        }\n\n        RollingNumber {\n            anchors.verticalCenter: parent.verticalCenter\n            visible: !root.compactCpu\n            value: Math.min(100, rootMod.percent)\n            digits: 2\n            suffix: \"%\"\n            tint: root.seal\n            family: root.mono\n            pixelSize: 12\n            rolling: root.motionDigits\n        }\n\n        IconText {"], ["            fill: 1\n        }\n\n        UiText {\n            anchors.verticalCenter: parent.verticalCenter\n            visible: root.compactCpu\n            text: String(Math.min(100, rootMod.percent)).padStart(2, '0') + \"%\"\n            color: root.seal\n            font.family: root.mono\n            font.pixelSize: 12\n        }\n    }\n", "            fill: 1\n        }\n\n        RollingNumber {\n            anchors.verticalCenter: parent.verticalCenter\n            visible: root.compactCpu\n            value: Math.min(100, rootMod.percent)\n            digits: 2\n            suffix: \"%\"\n            tint: root.seal\n            family: root.mono\n            pixelSize: 12\n            rolling: root.motionDigits\n        }\n    }\n"]], "modules/MemoryWidget.qml": [["            spacing: 0\n            anchors.verticalCenter: parent.verticalCenter\n\n            UiText {\n                text: rootMod.usedLabel\n                color: root.seal\n                font.family: root.mono\n                font.pixelSize: 12\n            }\n        }\n", "            spacing: 0\n            anchors.verticalCenter: parent.verticalCenter\n\n            RollingNumber {\n                value: Math.round(rootMod.usedGiB)\n                digits: 2\n                suffix: \"G\"\n                tint: root.seal\n                family: root.mono\n                pixelSize: 12\n                rolling: root.motionDigits\n            }\n        }\n"], ["            }\n        }\n\n        UiText {\n            anchors.verticalCenter: parent.verticalCenter\n            visible: root.compactMemory\n            text: rootMod.usedLabel\n            color: root.seal\n            font.family: root.mono\n            font.pixelSize: 12\n        }\n    }\n", "            }\n        }\n\n        RollingNumber {\n            anchors.verticalCenter: parent.verticalCenter\n            visible: root.compactMemory\n            value: Math.round(rootMod.usedGiB)\n            digits: 2\n            suffix: \"G\"\n            tint: root.seal\n            family: root.mono\n            pixelSize: 12\n            rolling: root.motionDigits\n        }\n    }\n"]], "modules/WorkspaceWidget.qml": [["\n    implicitWidth: wsRow.implicitWidth\n    implicitHeight: 28\n\n    // The focused workspace's number ONLY when it's a real workspace beyond\n    // the persist range \u2014 else 0. An int signals on value change only, so switching", "\n    implicitWidth: wsRow.implicitWidth\n    implicitHeight: 28\n\n    property real _lastPulse: 0\n    readonly property int focusedId: Hyprland.focusedWorkspace ? Number(Hyprland.focusedWorkspace.name) : 0\n    onFocusedIdChanged: {\n        var now = Date.now()\n        if (now - wsWidget._lastPulse < 450) return\n        wsWidget._lastPulse = now\n        root.barPulse(\"workspace\")\n    }\n\n    // The focused workspace's number ONLY when it's a real workspace beyond\n    // the persist range \u2014 else 0. An int signals on value change only, so switching"], ["        onClicked: root.workspaceVisible = !root.workspaceVisible\n    }\n\n    Row {\n        id: wsRow\n        anchors.centerIn: parent", "        onClicked: root.workspaceVisible = !root.workspaceVisible\n    }\n\n    property real cometX: 0\n    property real cometW: 0\n    property bool cometFwd: true\n    function aimComet(nx, nw) {\n        if (nx !== wsWidget.cometX) wsWidget.cometFwd = nx > wsWidget.cometX\n        wsWidget.cometX = nx\n        wsWidget.cometW = nw\n    }\n\n    Item {\n        id: cometLayer\n        visible: root.workspaceStyle === \"comet\"\n        anchors.fill: wsRow\n\n        property real lead: wsWidget.cometX + wsWidget.cometW\n        property real tail: wsWidget.cometX\n        property real ghostLead: wsWidget.cometX + wsWidget.cometW\n        property real ghostTail: wsWidget.cometX\n\n        Behavior on lead      { NumberAnimation { duration: wsWidget.cometFwd ? 190 : 430; easing.type: Easing.OutCubic } }\n        Behavior on tail      { NumberAnimation { duration: wsWidget.cometFwd ? 430 : 190; easing.type: Easing.OutCubic } }\n        Behavior on ghostLead { NumberAnimation { duration: wsWidget.cometFwd ? 320 : 620; easing.type: Easing.OutCubic } }\n        Behavior on ghostTail { NumberAnimation { duration: wsWidget.cometFwd ? 620 : 320; easing.type: Easing.OutCubic } }\n\n        Rectangle {\n            x: cometLayer.ghostTail\n            width: Math.max(2, cometLayer.ghostLead - cometLayer.ghostTail)\n            anchors.verticalCenter: parent.verticalCenter\n            height: 20\n            radius: root.styleRadiusSmall ? 5 : height / 2\n            color: Qt.rgba(root.seal.r, root.seal.g, root.seal.b, 0.10)\n        }\n\n        Rectangle {\n            x: cometLayer.tail\n            width: Math.max(2, cometLayer.lead - cometLayer.tail)\n            anchors.verticalCenter: parent.verticalCenter\n            height: 20\n            radius: root.styleRadiusSmall ? 5 : height / 2\n            color: Qt.rgba(root.seal.r, root.seal.g, root.seal.b, 0.26)\n            border.color: Qt.rgba(root.seal.r, root.seal.g, root.seal.b, 0.55)\n            border.width: 1\n        }\n    }\n\n    Row {\n        id: wsRow\n        anchors.centerIn: parent"], ["\n                readonly property bool isEmpty: !isFocused && !isOccupied\n\n                implicitWidth: root.workspaceStyle === \"numbers\" ? 22\n                             : root.workspaceStyle === \"magic\"   ? (isFocused ? 20 : 18)\n                             : (isFocused ? 32 : 16)\n                implicitHeight: 28", "\n                readonly property bool isEmpty: !isFocused && !isOccupied\n\n                onXChanged:         if (wsCell.isFocused) wsWidget.aimComet(wsCell.x, wsCell.width)\n                onWidthChanged:     if (wsCell.isFocused) wsWidget.aimComet(wsCell.x, wsCell.width)\n                onIsFocusedChanged: if (wsCell.isFocused) wsWidget.aimComet(wsCell.x, wsCell.width)\n                Component.onCompleted: if (wsCell.isFocused) wsWidget.aimComet(wsCell.x, wsCell.width)\n\n                implicitWidth: root.workspaceStyle === \"numbers\" ? 22\n                             : root.workspaceStyle === \"comet\"   ? 24\n                             : root.workspaceStyle === \"magic\"   ? (isFocused ? 20 : 18)\n                             : (isFocused ? 32 : 16)\n                implicitHeight: 28"], ["                    }\n                }\n\n                // \u2500\u2500 MAGIC style: the 3 ORIGINAL sparkle glyphs (filled / hollow / dot),\n                //    all forced into ONE font (Adwaita Mono has all three) so they share\n                //    a metric \u2192 no cross-font fallback misalignment \u2500\u2500", "                    }\n                }\n\n                Text {\n                    visible: root.workspaceStyle === \"comet\"\n                    anchors.centerIn: parent\n                    text: wsCell.wsId\n                    color: wsCell.isFocused  ? Qt.lighter(root.seal, 1.35)\n                         : wsCell.isOccupied ? Qt.rgba(root.ink.r, root.ink.g, root.ink.b, 0.75)\n                                             : Qt.rgba(root.ink.r, root.ink.g, root.ink.b, 0.3)\n                    font.family: root.mono\n                    font.pixelSize: 12\n                    font.weight: wsCell.isFocused ? Font.Bold : Font.Normal\n                    Behavior on color { ColorAnimation { duration: 200 } }\n                }\n\n                // \u2500\u2500 MAGIC style: the 3 ORIGINAL sparkle glyphs (filled / hollow / dot),\n                //    all forced into ONE font (Adwaita Mono has all three) so they share\n                //    a metric \u2192 no cross-font fallback misalignment \u2500\u2500"]], "panels/AiUsagePanel.qml": [["        x: Math.round(Math.max(6, Math.min(root.aiBarX - width / 2, parent.width - width - 6)))\n        y: root.barPosition === \"bottom\" ? (parent.height - barBottom - gap - height) : (barBottom + gap)\n        opacity: aiPanel.reveal\n        focus: root.aiUsageVisible\n\n        Keys.onPressed: function(event) {", "        x: Math.round(Math.max(6, Math.min(root.aiBarX - width / 2, parent.width - width - 6)))\n        y: root.barPosition === \"bottom\" ? (parent.height - barBottom - gap - height) : (barBottom + gap)\n        opacity: aiPanel.reveal\n        transformOrigin: root.barPosition === \"bottom\" ? Item.Bottom : Item.Top\n        scale: root.motionHover ? (0.955 + 0.045 * aiPanel.reveal) : 1\n        focus: root.aiUsageVisible\n\n        Keys.onPressed: function(event) {"]], "panels/BatteryPanel.qml": [["        x: Math.round(Math.max(6, Math.min(root.batteryBarX - width / 2, parent.width - width - 6)))\n        y: root.barPosition === \"bottom\" ? (parent.height - barBottom - gap - height) : (barBottom + gap)\n        opacity: batPanel.reveal\n        focus: root.batteryVisible\n\n        Keys.onPressed: function(event) {", "        x: Math.round(Math.max(6, Math.min(root.batteryBarX - width / 2, parent.width - width - 6)))\n        y: root.barPosition === \"bottom\" ? (parent.height - barBottom - gap - height) : (barBottom + gap)\n        opacity: batPanel.reveal\n        transformOrigin: root.barPosition === \"bottom\" ? Item.Bottom : Item.Top\n        scale: root.motionHover ? (0.955 + 0.045 * batPanel.reveal) : 1\n        focus: root.batteryVisible\n\n        Keys.onPressed: function(event) {"]], "panels/BluetoothPanel.qml": [["        x: Math.round(Math.max(6, Math.min(root.bluetoothBarX - width / 2, parent.width - width - 6)))\n        y: root.barPosition === \"bottom\" ? (parent.height - barBottom - gap - height) : (barBottom + gap)\n        opacity: btPanel.reveal\n        focus: root.bluetoothVisible\n\n        Keys.onPressed: function(event) {", "        x: Math.round(Math.max(6, Math.min(root.bluetoothBarX - width / 2, parent.width - width - 6)))\n        y: root.barPosition === \"bottom\" ? (parent.height - barBottom - gap - height) : (barBottom + gap)\n        opacity: btPanel.reveal\n        transformOrigin: root.barPosition === \"bottom\" ? Item.Bottom : Item.Top\n        scale: root.motionHover ? (0.955 + 0.045 * btPanel.reveal) : 1\n        focus: root.bluetoothVisible\n\n        Keys.onPressed: function(event) {"]], "panels/BrightnessPanel.qml": [["        x: Math.round(Math.max(6, Math.min(root.brightnessBarX - width / 2, parent.width - width - 6)))\n        y: root.barPosition === \"bottom\" ? (parent.height - barBottom - gap - height) : (barBottom + gap)\n        opacity: briPanel.reveal\n        focus: root.brightnessVisible\n\n        Keys.onPressed: function(event) {", "        x: Math.round(Math.max(6, Math.min(root.brightnessBarX - width / 2, parent.width - width - 6)))\n        y: root.barPosition === \"bottom\" ? (parent.height - barBottom - gap - height) : (barBottom + gap)\n        opacity: briPanel.reveal\n        transformOrigin: root.barPosition === \"bottom\" ? Item.Bottom : Item.Top\n        scale: root.motionHover ? (0.955 + 0.045 * briPanel.reveal) : 1\n        focus: root.brightnessVisible\n\n        Keys.onPressed: function(event) {"]], "panels/CalendarPopup.qml": [["        x: Math.round((parent.width - width) / 2)\n        y: root.barPosition === \"bottom\" ? (parent.height - barBottom - gap - height) : (barBottom + gap)\n        opacity: calPopup.reveal\n        focus: root.calendarVisible\n\n        Keys.onPressed: function(event) {", "        x: Math.round((parent.width - width) / 2)\n        y: root.barPosition === \"bottom\" ? (parent.height - barBottom - gap - height) : (barBottom + gap)\n        opacity: calPopup.reveal\n        transformOrigin: root.barPosition === \"bottom\" ? Item.Bottom : Item.Top\n        scale: root.motionHover ? (0.955 + 0.045 * calPopup.reveal) : 1\n        focus: root.calendarVisible\n\n        Keys.onPressed: function(event) {"]], "panels/ControlPanel.qml": [["        x: Math.round(Math.max(6, Math.min(root.launcherBarX - width / 2, parent.width - width - 6)))\n        y: root.barPosition === \"bottom\" ? (parent.height - barBottom - gap - height) : (barBottom + gap)\n        opacity: ctrlPanel.reveal\n        focus: root.controlVisible\n\n        Keys.onPressed: function(event) {", "        x: Math.round(Math.max(6, Math.min(root.launcherBarX - width / 2, parent.width - width - 6)))\n        y: root.barPosition === \"bottom\" ? (parent.height - barBottom - gap - height) : (barBottom + gap)\n        opacity: ctrlPanel.reveal\n        transformOrigin: root.barPosition === \"bottom\" ? Item.Bottom : Item.Top\n        scale: root.motionHover ? (0.955 + 0.045 * ctrlPanel.reveal) : 1\n        focus: root.controlVisible\n\n        Keys.onPressed: function(event) {"], ["                    readonly property var opts: [\n                        { label: \"Default\", mode: \"default\" },\n                        { label: \"Numbers\", mode: \"numbers\" },\n                        { label: \"Magic\",   mode: \"magic\"   }\n                    ]\n                    Repeater {\n                        model: wsStyleRow.opts", "                    readonly property var opts: [\n                        { label: \"Default\", mode: \"default\" },\n                        { label: \"Numbers\", mode: \"numbers\" },\n                        { label: \"Magic\",   mode: \"magic\"   },\n                        { label: \"Comet\",   mode: \"comet\"   }\n                    ]\n                    Repeater {\n                        model: wsStyleRow.opts"], ["\n            Rectangle { width: parent.width; height: 1; color: root.sep }\n\n            // \u2500\u2500 POSITION (bar on top or bottom edge) \u2500\u2500\n            UiText {\n                text: \"POSITION\"", "\n            Rectangle { width: parent.width; height: 1; color: root.sep }\n\n            // \u2500\u2500 MOTION \u2500\u2500\n            UiText {\n                text: \"MOTION\"\n                color: root.sumiHi; font.family: root.mono; font.pixelSize: 10; font.letterSpacing: 1\n            }\n            Row {\n                width: parent.width; spacing: 4\n                Tile { width: root.evenW((wwCol.width - 8) / 3); label: \"Hover\";  active: root.motionHover;  onActivated: root.motionHover = !root.motionHover }\n                Tile { width: root.evenW((wwCol.width - 8) / 3); label: \"Sweep\";  active: root.motionSweep;  onActivated: root.motionSweep = !root.motionSweep }\n                Tile { width: root.evenW((wwCol.width - 8) / 3); label: \"Digits\"; active: root.motionDigits; onActivated: root.motionDigits = !root.motionDigits }\n            }\n\n            Rectangle { width: parent.width; height: 1; color: root.sep }\n\n            // \u2500\u2500 POSITION (bar on top or bottom edge) \u2500\u2500\n            UiText {\n                text: \"POSITION\""]], "panels/CpuPanel.qml": [["        x: Math.round(Math.max(6, Math.min(root.cpuBarX - width / 2, parent.width - width - 6)))\n        y: root.barPosition === \"bottom\" ? (parent.height - barBottom - gap - height) : (barBottom + gap)\n        opacity: cpuPanel.reveal\n        focus: root.cpuVisible\n\n        Keys.onPressed: function(event) {", "        x: Math.round(Math.max(6, Math.min(root.cpuBarX - width / 2, parent.width - width - 6)))\n        y: root.barPosition === \"bottom\" ? (parent.height - barBottom - gap - height) : (barBottom + gap)\n        opacity: cpuPanel.reveal\n        transformOrigin: root.barPosition === \"bottom\" ? Item.Bottom : Item.Top\n        scale: root.motionHover ? (0.955 + 0.045 * cpuPanel.reveal) : 1\n        focus: root.cpuVisible\n\n        Keys.onPressed: function(event) {"]], "panels/GithubPanel.qml": [["            ? (parent.height - ghPanel.barBottom - ghPanel.gap - height)\n            : (ghPanel.barBottom + ghPanel.gap)\n        opacity: ghPanel.reveal\n        focus: ghPanel.root.githubVisible\n\n        Keys.onPressed: function (event) {", "            ? (parent.height - ghPanel.barBottom - ghPanel.gap - height)\n            : (ghPanel.barBottom + ghPanel.gap)\n        opacity: ghPanel.reveal\n        transformOrigin: root.barPosition === \"bottom\" ? Item.Bottom : Item.Top\n        scale: root.motionHover ? (0.955 + 0.045 * ghPanel.reveal) : 1\n        focus: ghPanel.root.githubVisible\n\n        Keys.onPressed: function (event) {"]], "panels/GpuPanel.qml": [["        x: Math.round(Math.max(6, Math.min(root.gpuBarX - width / 2, parent.width - width - 6)))\n        y: root.barPosition === \"bottom\" ? (parent.height - barBottom - gap - height) : (barBottom + gap)\n        opacity: gpuPanel.reveal\n        focus: root.gpuVisible\n\n        Keys.onPressed: function(event) {", "        x: Math.round(Math.max(6, Math.min(root.gpuBarX - width / 2, parent.width - width - 6)))\n        y: root.barPosition === \"bottom\" ? (parent.height - barBottom - gap - height) : (barBottom + gap)\n        opacity: gpuPanel.reveal\n        transformOrigin: root.barPosition === \"bottom\" ? Item.Bottom : Item.Top\n        scale: root.motionHover ? (0.955 + 0.045 * gpuPanel.reveal) : 1\n        focus: root.gpuVisible\n\n        Keys.onPressed: function(event) {"]], "panels/KeybindsPanel.qml": [["            ? (parent.height - kbPanel.barBottom - kbPanel.gap - height)\n            : (kbPanel.barBottom + kbPanel.gap)\n        opacity: kbPanel.reveal\n        focus: root.keybindsVisible\n        clip: true\n", "            ? (parent.height - kbPanel.barBottom - kbPanel.gap - height)\n            : (kbPanel.barBottom + kbPanel.gap)\n        opacity: kbPanel.reveal\n        transformOrigin: root.barPosition === \"bottom\" ? Item.Bottom : Item.Top\n        scale: root.motionHover ? (0.955 + 0.045 * kbPanel.reveal) : 1\n        focus: root.keybindsVisible\n        clip: true\n"]], "panels/LanguagePanel.qml": [["        x: Math.round(Math.max(6, Math.min(root.languageBarX - width / 2, parent.width - width - 6)))\n        y: root.barPosition === \"bottom\" ? (parent.height - barBottom - gap - height) : (barBottom + gap)\n        opacity: langPanel.reveal\n        focus: root.langVisible\n\n        Keys.onPressed: function(event) {", "        x: Math.round(Math.max(6, Math.min(root.languageBarX - width / 2, parent.width - width - 6)))\n        y: root.barPosition === \"bottom\" ? (parent.height - barBottom - gap - height) : (barBottom + gap)\n        opacity: langPanel.reveal\n        transformOrigin: root.barPosition === \"bottom\" ? Item.Bottom : Item.Top\n        scale: root.motionHover ? (0.955 + 0.045 * langPanel.reveal) : 1\n        focus: root.langVisible\n\n        Keys.onPressed: function(event) {"]], "panels/MemoryPanel.qml": [["        x: Math.round(Math.max(6, Math.min(root.memoryBarX - width / 2, parent.width - width - 6)))\n        y: root.barPosition === \"bottom\" ? (parent.height - barBottom - gap - height) : (barBottom + gap)\n        opacity: memPanel.reveal\n        focus: root.memVisible\n\n        Keys.onPressed: function(event) {", "        x: Math.round(Math.max(6, Math.min(root.memoryBarX - width / 2, parent.width - width - 6)))\n        y: root.barPosition === \"bottom\" ? (parent.height - barBottom - gap - height) : (barBottom + gap)\n        opacity: memPanel.reveal\n        transformOrigin: root.barPosition === \"bottom\" ? Item.Bottom : Item.Top\n        scale: root.motionHover ? (0.955 + 0.045 * memPanel.reveal) : 1\n        focus: root.memVisible\n\n        Keys.onPressed: function(event) {"]], "panels/MprisPanel.qml": [["        x: Math.round(Math.max(6, Math.min(root.mprisBarX - width / 2, parent.width - width - 6)))\n        y: root.barPosition === \"bottom\" ? (parent.height - barBottom - gap - height) : (barBottom + gap)\n        opacity: mprisPanel.reveal\n        focus: root.mprisVisible\n\n        Keys.onPressed: function(event) {", "        x: Math.round(Math.max(6, Math.min(root.mprisBarX - width / 2, parent.width - width - 6)))\n        y: root.barPosition === \"bottom\" ? (parent.height - barBottom - gap - height) : (barBottom + gap)\n        opacity: mprisPanel.reveal\n        transformOrigin: root.barPosition === \"bottom\" ? Item.Bottom : Item.Top\n        scale: root.motionHover ? (0.955 + 0.045 * mprisPanel.reveal) : 1\n        focus: root.mprisVisible\n\n        Keys.onPressed: function(event) {"]], "panels/NetworkPanel.qml": [["        x: Math.round(Math.max(6, Math.min(root.networkBarX - width / 2, parent.width - width - 6)))\n        y: root.barPosition === \"bottom\" ? (parent.height - barBottom - gap - height) : (barBottom + gap)\n        opacity: netPanel.reveal\n        focus: root.networkVisible\n\n        Keys.onPressed: function(event) {", "        x: Math.round(Math.max(6, Math.min(root.networkBarX - width / 2, parent.width - width - 6)))\n        y: root.barPosition === \"bottom\" ? (parent.height - barBottom - gap - height) : (barBottom + gap)\n        opacity: netPanel.reveal\n        transformOrigin: root.barPosition === \"bottom\" ? Item.Bottom : Item.Top\n        scale: root.motionHover ? (0.955 + 0.045 * netPanel.reveal) : 1\n        focus: root.networkVisible\n\n        Keys.onPressed: function(event) {"]], "panels/NotificationPanel.qml": [["        x: Math.round(Math.max(6, Math.min(root.notifBarX, parent.width - width - 6)))\n        y: root.barPosition === \"bottom\" ? (parent.height - barBottom - gap - height) : (barBottom + gap)\n        opacity: notifPanel.reveal\n        focus: root.notifVisible\n\n        Keys.onPressed: function(event) {", "        x: Math.round(Math.max(6, Math.min(root.notifBarX, parent.width - width - 6)))\n        y: root.barPosition === \"bottom\" ? (parent.height - barBottom - gap - height) : (barBottom + gap)\n        opacity: notifPanel.reveal\n        transformOrigin: root.barPosition === \"bottom\" ? Item.Bottom : Item.Top\n        scale: root.motionHover ? (0.955 + 0.045 * notifPanel.reveal) : 1\n        focus: root.notifVisible\n\n        Keys.onPressed: function(event) {"]], "panels/PowerProfilePanel.qml": [["        x: Math.round(Math.max(6, Math.min(root.powerBarX - width / 2, parent.width - width - 6)))\n        y: root.barPosition === \"bottom\" ? (parent.height - barBottom - gap - height) : (barBottom + gap)\n        opacity: profilePanel.reveal\n        focus: root.powerProfileVisible\n\n        Keys.onPressed: function(event) {", "        x: Math.round(Math.max(6, Math.min(root.powerBarX - width / 2, parent.width - width - 6)))\n        y: root.barPosition === \"bottom\" ? (parent.height - barBottom - gap - height) : (barBottom + gap)\n        opacity: profilePanel.reveal\n        transformOrigin: root.barPosition === \"bottom\" ? Item.Bottom : Item.Top\n        scale: root.motionHover ? (0.955 + 0.045 * profilePanel.reveal) : 1\n        focus: root.powerProfileVisible\n\n        Keys.onPressed: function(event) {"]], "panels/StoragePanel.qml": [["        x: Math.round(Math.max(6, Math.min(root.storageBarX - width / 2, parent.width - width - 6)))\n        y: root.barPosition === \"bottom\" ? (parent.height - barBottom - gap - height) : (barBottom + gap)\n        opacity: storagePanel.reveal\n        focus: root.storageVisible\n\n        Keys.onPressed: function(event) {", "        x: Math.round(Math.max(6, Math.min(root.storageBarX - width / 2, parent.width - width - 6)))\n        y: root.barPosition === \"bottom\" ? (parent.height - barBottom - gap - height) : (barBottom + gap)\n        opacity: storagePanel.reveal\n        transformOrigin: root.barPosition === \"bottom\" ? Item.Bottom : Item.Top\n        scale: root.motionHover ? (0.955 + 0.045 * storagePanel.reveal) : 1\n        focus: root.storageVisible\n\n        Keys.onPressed: function(event) {"]], "panels/ThermalsPanel.qml": [["        x: Math.round(Math.max(6, Math.min(root.thermalBarX - width / 2, parent.width - width - 6)))\n        y: root.barPosition === \"bottom\" ? (parent.height - barBottom - gap - height) : (barBottom + gap)\n        opacity: thermalsPanel.reveal\n        focus: root.thermalVisible\n\n        Keys.onPressed: function(event) {", "        x: Math.round(Math.max(6, Math.min(root.thermalBarX - width / 2, parent.width - width - 6)))\n        y: root.barPosition === \"bottom\" ? (parent.height - barBottom - gap - height) : (barBottom + gap)\n        opacity: thermalsPanel.reveal\n        transformOrigin: root.barPosition === \"bottom\" ? Item.Bottom : Item.Top\n        scale: root.motionHover ? (0.955 + 0.045 * thermalsPanel.reveal) : 1\n        focus: root.thermalVisible\n\n        Keys.onPressed: function(event) {"]], "panels/TrayMenu.qml": [["        x: parent ? Math.max(6, Math.min(root.trayMenuX, parent.width - width - 6)) : 6\n        y: root.barPosition === \"bottom\" ? (parent.height - barBottom - gap - height) : (barBottom + gap)\n        opacity: trayMenu.reveal\n        focus: root.trayMenuVisible\n\n        Keys.onPressed: function(event) {", "        x: parent ? Math.max(6, Math.min(root.trayMenuX, parent.width - width - 6)) : 6\n        y: root.barPosition === \"bottom\" ? (parent.height - barBottom - gap - height) : (barBottom + gap)\n        opacity: trayMenu.reveal\n        transformOrigin: root.barPosition === \"bottom\" ? Item.Bottom : Item.Top\n        scale: root.motionHover ? (0.955 + 0.045 * trayMenu.reveal) : 1\n        focus: root.trayMenuVisible\n\n        Keys.onPressed: function(event) {"]], "panels/TrayPanel.qml": [["        x: Math.round(Math.max(6, Math.min(root.trayBarX, parent.width - width - 6)))\n        y: root.barPosition === \"bottom\" ? (parent.height - barBottom - gap - height) : (barBottom + gap)\n        opacity: trayPanel.reveal\n        focus: root.trayVisible\n\n        Keys.onPressed: function(event) {", "        x: Math.round(Math.max(6, Math.min(root.trayBarX, parent.width - width - 6)))\n        y: root.barPosition === \"bottom\" ? (parent.height - barBottom - gap - height) : (barBottom + gap)\n        opacity: trayPanel.reveal\n        transformOrigin: root.barPosition === \"bottom\" ? Item.Bottom : Item.Top\n        scale: root.motionHover ? (0.955 + 0.045 * trayPanel.reveal) : 1\n        focus: root.trayVisible\n\n        Keys.onPressed: function(event) {"]], "panels/VolumePanel.qml": [["        x: Math.round(Math.max(6, Math.min(root.volumeBarX - width / 2, parent.width - width - 6)))\n        y: root.barPosition === \"bottom\" ? (parent.height - barBottom - gap - height) : (barBottom + gap)\n        opacity: volPanel.reveal\n        focus: root.volVisible\n\n        Keys.onPressed: function(event) {", "        x: Math.round(Math.max(6, Math.min(root.volumeBarX - width / 2, parent.width - width - 6)))\n        y: root.barPosition === \"bottom\" ? (parent.height - barBottom - gap - height) : (barBottom + gap)\n        opacity: volPanel.reveal\n        transformOrigin: root.barPosition === \"bottom\" ? Item.Bottom : Item.Top\n        scale: root.motionHover ? (0.955 + 0.045 * volPanel.reveal) : 1\n        focus: root.volVisible\n\n        Keys.onPressed: function(event) {"]], "panels/WeatherPanel.qml": [["        x: Math.round(Math.max(6, Math.min(root.weatherBarX - width / 2, parent.width - width - 6)))\n        y: root.barPosition === \"bottom\" ? (parent.height - barBottom - gap - height) : (barBottom + gap)\n        opacity: wxPanel.reveal\n        focus: root.weatherVisible\n\n        Keys.onPressed: function(event) {", "        x: Math.round(Math.max(6, Math.min(root.weatherBarX - width / 2, parent.width - width - 6)))\n        y: root.barPosition === \"bottom\" ? (parent.height - barBottom - gap - height) : (barBottom + gap)\n        opacity: wxPanel.reveal\n        transformOrigin: root.barPosition === \"bottom\" ? Item.Bottom : Item.Top\n        scale: root.motionHover ? (0.955 + 0.045 * wxPanel.reveal) : 1\n        focus: root.weatherVisible\n\n        Keys.onPressed: function(event) {"]], "panels/WorkspacePanel.qml": [["        x: Math.round(Math.max(6, Math.min(root.workspaceBarX - width / 2, parent.width - width - 6)))\n        y: root.barPosition === \"bottom\" ? (parent.height - barBottom - gap - height) : (barBottom + gap)\n        opacity: wsPanel.reveal\n        focus: root.workspaceVisible\n\n        Keys.onPressed: function(event) {", "        x: Math.round(Math.max(6, Math.min(root.workspaceBarX - width / 2, parent.width - width - 6)))\n        y: root.barPosition === \"bottom\" ? (parent.height - barBottom - gap - height) : (barBottom + gap)\n        opacity: wsPanel.reveal\n        transformOrigin: root.barPosition === \"bottom\" ? Item.Bottom : Item.Top\n        scale: root.motionHover ? (0.955 + 0.045 * wsPanel.reveal) : 1\n        focus: root.workspaceVisible\n\n        Keys.onPressed: function(event) {"]]}}"""


def main():
    if not RISE.is_dir():
        print(f"bar-motion: {RISE} not found", file=sys.stderr)
        return 1
    data = json.loads(PAYLOAD)
    plan, fresh, fail = [], 0, []

    for rel, content in data["new"].items():
        path = RISE / rel
        if path.exists() and path.read_text() == content:
            fresh += 1
        else:
            plan.append((path, content, rel, 0))

    for rel, hunks in data["edits"].items():
        path = RISE / rel
        if not path.is_file():
            fail.append(f"{rel}: file missing")
            continue
        src = path.read_text()
        applied = 0
        for old, new in hunks:
            if src.count(new) == 1:
                continue
            n = src.count(old)
            if n != 1:
                fail.append(f"{rel}: anchor matched {n}x, expected 1")
                break
            src = src.replace(old, new)
            applied += 1
        else:
            if applied:
                plan.append((path, src, rel, applied))
            else:
                fresh += 1

    if fail:
        print("bar-motion: aborted, NOTHING written", file=sys.stderr)
        for f in fail:
            print(f"  {f}", file=sys.stderr)
        print("\nthese files have drifted from the commit this was built against;", file=sys.stderr)
        print("reconcile them by hand, then re-run", file=sys.stderr)
        return 1

    for path, content, rel, applied in plan:
        path.parent.mkdir(parents=True, exist_ok=True)
        path.write_text(content)
        print(f"  {'patch' if applied else 'write'}  {rel}"
              + (f"  ({applied} hunk{'s' if applied > 1 else ''})" if applied else ""))

    print(f"\nbar-motion: {len(plan)} changed, {fresh} already current")
    return 0


if __name__ == "__main__":
    sys.exit(main())
