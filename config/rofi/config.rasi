@import "~/.local/state/dots/theme/rofi-colors.rasi"
configuration {

    modi: "drun,run";
    font: "JetBrainsMono Nerd Font 10";
    show-icons: true;
    icon-theme: "Papirus";
    display-drun: "☠ Summon";
    display-run: "⚕ Conjure ";
    display-window: "⚧ Portal ";
    drun-display-format: "{icon} {name}";
    sidebar-mode: true;
    drun-show-actions: false;
    disable-history: false;
}

/* Palette comes from the theme (imported at the top of this file).
   Do NOT hardcode colours here — later definitions win and the theme
   would silently stop applying, which is exactly what used to happen. */
* {
    bg-col-alpha:       @bg-col;
    bg-col-alt:         @bg-col-light;
    pink:               @urgent;
    green:              @ok;

    normal-background:           transparent;
    normal-foreground:           @fg-col;

    active-background:           @bg-col-light;
    active-foreground:           @accent;

    urgent-background:           @urgent;
    urgent-foreground:           @fg-col;

    alternate-normal-background: transparent;
    alternate-normal-foreground: @fg-col;

    selected-normal-background:  @accent;
    selected-normal-foreground:  @bg-col;

    selected-urgent-background:  @urgent;
    selected-urgent-foreground:  @bg-col;

    selected-active-background:  @accent;
    selected-active-foreground:  @bg-col;

    spacing: 2;
}

/* Window */
window {
    background-color: @bg-col-alpha; /* Using the alpha variable for transparency */
    border: 3px;
    border-color: @border-col;       /* Now transparent */
    border-radius: 8px;
    padding: 20px;
    transparency: "real";
    width: 55%;
    height: 45%;
    location: center;
}

/* Main layout */
mainbox {
    border: 0;
    padding: 0;
    spacing: 8px;
    background-color: transparent;
    children: [ "horibox" ];
}

horibox {
    orientation: horizontal;
    spacing: 15px;
    children: [ "image-box", "vertical-box" ];
}

/* Image box */
image-box {
    background-image: url("ab67616d0000b273d62a6267448745ca64db99ea.jpeg", both);
    width: 440px;
    height: 440px;
    border: 3px;
    border-color: @accent;
    border-radius: 5px;
    expand: false;
    background-color: rgba(0,0,0,0.35);
}

/* Right-side vertical layout */
vertical-box {
    orientation: vertical;
    spacing: 2px;
    expand: true;
    width: 55px;
    children: [ "inputbar", "listview", "mode-switcher" ];
}

/* Messages */
message {
    border: 2px 0px 0px;
    border-color: @border-col;
    padding: 8px;
    text-color: @pink;
}

textbox {
    text-color: @fg-col;
}

/* Input Bar */
inputbar {
    children: [ prompt, textbox-prompt-colon, entry ];
    background-color: transparent;
    border: 3px;
    border-color: @bg-col-light;
    border-radius: 5px;
    margin: 0px 0px 8px 0px;
    padding: 8px 12px;
    vertical-align: 0.5;
}

textbox-prompt-colon {
    expand: false;
    str: "⸸";
    margin: 0px 8px 0px 0px;
    text-color: @accent;
    font: "URW Gothic Bold 14";
    vertical-align: 0.4;
    horizontal-align: 0.5;
    padding: 4px 0px 6px 0px;
}

/* List View */
listview {
    fixed-height: false;
    border: 0px;
    spacing: 3px;
    scrollbar: false;
    padding: 6px 4px;
    lines: 5;
    columns: 1;
    dynamic: true;
    background-color: transparent;
}

/* Elements (Rows) */
element {
    border: 0;
    padding: 6px 10px;
    border-radius: 0px;
    spacing: 4px;
    orientation: horizontal;
    children: [ element-icon, element-text ];
    background-color: transparent;
    text-color: @fg-col;
}

element-icon {
    size: 24px;
    background-color: inherit;
    horizontal-align: 0.0;
    vertical-align: 0.5;
    padding: 0px 8px 0px 0px;
}

element.normal.normal {
    background-color: @normal-background;
    text-color: @normal-foreground;
}

element.normal.urgent {
    background-color: @urgent-background;
    text-color: @urgent-foreground;
}

element.normal.active {
    background-color: @active-background;
    text-color: @active-foreground;
}

element.selected.normal {
    background-color: @accent;
    text-color: @bg-col;
}

element.selected.urgent {
    background-color: @selected-urgent-background;
    text-color: @selected-urgent-foreground;
}

element.selected.active {
    background-color: @selected-active-background;
    text-color: @selected-active-foreground;
}

element.alternate.normal {
    background-color: @alternate-normal-background;
    text-color: @alternate-normal-foreground;
}

element-text {
    background-color: inherit;
    text-color: inherit;
    vertical-align: 0.5;
    horizontal-align: 0.0;
    padding: 0px 5px;
    highlight: bold;
    font: "URW Gothic 11";
}

/* Scrollbar + Sidebar */
scrollbar {
    width: 4px;
    border: 0;
    handle-width: 8px;
    padding: 0;
    handle-color: @accent;
}

sidebar {
    border: 0;
    spacing: 6px;
    background-color: transparent;
}

/* Buttons */
button {
    padding: 8px;
    border-radius: 0px;
    text-color: @fg-col;
    horizontal-align: 0.5;
}

button.selected {
    background-color: @accent;
    text-color: @bg-col;
}

/* Entry */
entry {
    background-color: inherit;
    spacing: 0;
    text-color: @fg-col;
    placeholder: "☠ Summon the forbidden...";
    placeholder-color: rgba(116,103,94,0.6);
    cursor: text;
    font: "URW Gothic 12";
    vertical-align: 0.5;
    horizontal-align: 0.0;
    padding: 2px 0px;
}

prompt {
    enabled: true;
    padding: 8px;
    spacing: 0;
    text-color: @accent;
    font: "URW Gothic Bold 13";
    vertical-align: 0.5;
    horizontal-align: 0.5;
}

mode-switcher {
    enabled: true;
    spacing: 12px;
    margin: 0px 0px 10px 0px;
    background-color: transparent;
}
