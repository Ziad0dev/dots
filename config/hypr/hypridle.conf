# ⸸ hypridle — idle management ⸸
# System suspend is disabled in configuration.nix; this only locks + blanks.

general {
    lock_cmd = pidof hyprlock || hyprlock --grace 5   # single instance; grace is a CLI flag since 0.9.x
    before_sleep_cmd = loginctl lock-session
    after_sleep_cmd = hyprctl dispatch dpms on
}

# Lock after 10 minutes idle
listener {
    timeout = 600
    on-timeout = loginctl lock-session
}

# Screens off after 15 minutes
listener {
    timeout = 900
    on-timeout = hyprctl dispatch dpms off
    on-resume = hyprctl dispatch dpms on
}
