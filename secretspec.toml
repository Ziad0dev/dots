[project]
name = "dots"
revision = "1.0"

[providers]
pass = "pass://"
bw = "bw://"

[profiles.default]
LASTFM_PASSWORD = { description = "last.fm password, consumed by mpdscribble at login", required = true, providers = ["pass"] }
GOOGLE_BOOKS_API_KEY = { description = "optional, improves calibre-web metadata", required = false, providers = ["bw", "pass"] }
